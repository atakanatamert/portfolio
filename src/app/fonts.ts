import { Space_Mono, Orbitron, Poppins, Montserrat } from "next/font/google";

export const orbitron = Orbitron({
    weight: "400",
    subsets: ["latin"],
});

export const space_mono = Space_Mono({
    weight: "400",
    subsets: ["latin"],
});

export const poppins = Poppins({
    weight: "400",
    subsets: ["latin"],
});

export const montserrat = Montserrat({
    weight: "300",
    subsets: ["latin"],
});
